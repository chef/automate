#!/usr/bin/env bash

[ -d "./test/vendor/bundle" ] || (cd test && $(hab pkg path core/ruby3_4)/bin/bundle install --path vendor/bundle)
cd test && $(hab pkg path core/ruby3_4)/bin/bundle exec $(hab pkg path core/ruby3_4)/bin/ruby lib/smoke-test "$@"
