# By default we disable ssl validation by default.
# If you have valid certificates in Elasticsearch and the fqdn of the cert
# connects to localhost on the ES nodes, set ssl_no_validate to False.

use_ssl = "True"
ssl_no_validate = "True"
