extends: substitution
message: "Use '%s' instead of '%s'"
level: error
ignorecase: false
swap:
  '(?:[^\s]*) ?Automate': Chef Automate
  'Chef Automate Integration App': Chef Automate Integration App
ignore:

