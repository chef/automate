{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "Validate request that accepts filters, group_by and a range",

  "properties": {
    "filter": { "$ref": "base#/definitions/must_should_filter" },
    "group_by": {
      "enum": [ "status" ]
    },
    "range": { "$ref": "base#/definitions/range_with_interval" }
  },
  "required": ["range"]
}
