{
  "paths": {
    "/compliance/reporting/export": {
      "get": {
        "summary": "Export reports",
        "description": "Stream reports in JSON (default) or CSV format. Use the value 'csv' for the type parameter in the request to receive CSV formatted data.",
        "operationId": "Export",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/v1ExportData"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/v1Query"
            }
          }
        ],
        "tags": [
          "Compliance Reporting Export"
        ],
        "produces": [
          "application/json",
          "text/csv"
        ]
      }
    }
  }
}
