package policy_test

import (
	"fmt"
	"go/parser"
	"go/token"
	"path/filepath"
	"testing"

	plugin "github.com/golang/protobuf/protoc-gen-go/plugin"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"

	"github.com/chef/automate/lib/grpc/grpctest"
)

// /!\ Warning /!\
//
// Due to the way protoc-gen-star works, any errors occurring from its internal
// use of proto-gen-go (it's embedding that, running it, and then discarding the
// output) HIDE LOG MESSAGES emitted via `t.Logf`, or `t.Errorf`, in any of the
// helpers.
//
// So, when you're trying to get behind a test failure that ends with a line
// that starts with `protoc-gen-go:`, you'll have to resort to aborting the test
// using `t.Fatalf` to see your debug output.`

const a2Root = "github.com/chef/automate"

func TestCreatesValidCode(t *testing.T) {
	testdata := func(baseFileName string) string {
		return fmt.Sprintf("components/automate-grpc/protoc-gen-policy/testdata/%s.proto", baseFileName)
	}
	fullPath := func(baseFileName, pkgName, vsn string) string {
		return filepath.Join(a2Root,
			fmt.Sprintf("components/mock-service/%s/%s.pb.policy-%s.go", pkgName, baseFileName, vsn))
	}

	fullPathV1 := func(baseFileName, pkgName string) string {
		return fullPath(baseFileName, pkgName, "v1")
	}

	fullPathV2 := func(baseFileName, pkgName string) string {
		return fullPath(baseFileName, pkgName, "v2")
	}

	tests := map[string]struct {
		filename string
		checks   []checkFunc
	}{
		"no services defined": {
			testdata("no_services"),
			check(noError, outputFiles(0))},

		"service without methods": {
			testdata("service_without_methods"),
			check(noError, outputFiles(0))},

		"service with one method": {
			testdata("one_method"),
			check(noError,
				outputFiles(1),
				syntacticallyCorrectFile(fullPathV1("one_method", "mock")),
				fileWithContent(fullPathV1("one_method", "mock"), "// Code generated by protoc-gen-policy.",
					"// source: "+testdata("one_method"),
					"package mock", // matches go_package
					`policy.MapMethodTo("/chef.automate.domain.one_method.Mock/MockMethod", `+
						`"mock:foo:{bar}", "action", "", "", func(unexpandedResource string, input interface{}) string {`,
					"input.(*Foo)", // check that there's now circular import
				),
			)},

		"service with one method plus http annotation": {
			testdata("one_method_plus_http"),
			check(noError,
				outputFiles(1),
				syntacticallyCorrectFile(fullPathV1("one_method_plus_http", "mock")),
				fileWithContent(fullPathV1("one_method_plus_http", "mock"), "// Code generated by protoc-gen-policy.",
					"// source: "+testdata("one_method_plus_http"),
					"package mock",
					`policy.MapMethodTo("/chef.automate.domain.one_method.Mock/MockMethod", "mock:foo:{bar}", "action", `+
						`"GET", "/auth/foo/baz", func(unexpandedResource string, input interface{}) string {`),
			)},

		"service with one method having v2 annotations only": {
			testdata("one_method_v2_only"),
			check(noError,
				outputFiles(1),
				syntacticallyCorrectFile(fullPathV2("one_method_v2_only", "mock")),
				fileWithContent(fullPathV2("one_method_v2_only", "mock"), "// Code generated by protoc-gen-policy.",
					"// source: "+testdata("one_method_v2_only"),
					"package mock", // matches go_package

					// Note: the code generator renames the import: policy_v2 -> policyv2
					`policyv2.MapMethodTo("/chef.automate.domain.one_method.Mock/MockMethod", `+
						`"mockvtwo:foo:{bar}", "action:vtwo:verb", "", "", func(unexpandedResource string, input interface{}) string {`,
					"input.(*Foo)", // check that there's now circular import
				),
			)},

		"service with one method having both v1 and v2 annotations": {
			testdata("one_method_v1_and_v2"),
			check(noError,
				outputFiles(2),
				syntacticallyCorrectFile(fullPathV1("one_method_v1_and_v2", "mock")),
				syntacticallyCorrectFile(fullPathV2("one_method_v1_and_v2", "mock")),
				fileWithContent(fullPathV1("one_method_v1_and_v2", "mock"), "// Code generated by protoc-gen-policy.",
					"// source: "+testdata("one_method_v1_and_v2"),
					"package mock", // matches go_package

					// Note: the code generator renames the import: policy_v2 -> policyv2
					`policy.MapMethodTo("/chef.automate.domain.one_method.Mock/MockMethod", "mock:foo:{bar}", "action", `+
						`"GET", "/auth/foo/baz", func(unexpandedResource string, input interface{}) string {`,
					"input.(*Foo)", // check that there's now circular import
				),
				fileWithContent(fullPathV2("one_method_v1_and_v2", "mock"), "// Code generated by protoc-gen-policy.",
					"// source: "+testdata("one_method_v1_and_v2"),
					"package mock", // matches go_package

					// Note: the code generator renames the import: policy_v2 -> policyv2
					`policyv2.MapMethodTo("/chef.automate.domain.one_method.Mock/MockMethod", "mockvtwo:foo:{bar}", "action:vtwo:verb", `+
						`"GET", "/auth/foo/baz", func(unexpandedResource string, input interface{}) string {`,
					"input.(*Foo)", // check that there's now circular import
				),
			)},

		"service with v2 resource pattern annotation that doesn't match message fields": {
			testdata("resource_field_name_mismatch"),
			check(someError)},

		"service with both v1 and v2 annotations incomplete": {
			testdata("partial_v1_and_partial_v2"),
			check(someError)},

		"service with invalid v2 annotations": {
			testdata("invalid_v2"),
			check(someError)},

		"service with full v1 but partial v2 annotations": {
			testdata("full_v1_with_partial_v2"),
			check(noError,
				outputFiles(1), // v2 fails, leaving 1 instead of 2 files
				syntacticallyCorrectFile(fullPathV1("full_v1_with_partial_v2", "mock")),
				fileWithContent(fullPathV1("full_v1_with_partial_v2", "mock"), "// Code generated by protoc-gen-policy.",
					"// source: "+testdata("full_v1_with_partial_v2"),
					"package mock", // matches go_package

					// Note: the code generator renames the import: policy_v2 -> policyv2
					`policy.MapMethodTo("/chef.automate.domain.one_method.Mock/MockMethodV1Only", "mock:foo:{bar}", "action", `+
						`"GET", "/auth/foo/baz", func(unexpandedResource string, input interface{}) string {`,
					`policy.MapMethodTo("/chef.automate.domain.one_method.Mock/MockMethodV1AndV2", "mock:foo:{bar}:v1-and-v2", "action-one", `+
						`"DELETE", "/auth/foo/baz", func(unexpandedResource string, input interface{}) string {`,
					"input.(*Foo)", // check that there's now circular import
				),
			)},

		"service whose go_package name contains underscore": {
			testdata("pkg_name_with_underscore"),
			check(noError,
				outputFiles(1),
				syntacticallyCorrectFile(fullPathV1("pkg_name_with_underscore", "pkg_name_with_underscore")),
				fileWithContent(fullPathV1("pkg_name_with_underscore", "pkg_name_with_underscore"), "// Code generated by protoc-gen-policy.",
					"// source: "+testdata("pkg_name_with_underscore"),
					"package pkg_name_with_underscore"),
			)},

		"service with imported method arguments": {
			testdata("imported_method_arguments"),
			check(noError,
				outputFiles(1),
				syntacticallyCorrectFile(fullPathV1("imported_method_arguments", "mock")),
				fileWithContent(fullPathV1("imported_method_arguments", "mock"), "// Code generated by protoc-gen-policy.",
					"// source: "+testdata("imported_method_arguments"),
					"package mock",
					`messages "github.com/chef/automate/components/mock-service/mock/messages"`,
					"input.(*messages.Foo)"),
			)},
	}

	// Each call to GenerateCode (via runWithInputFile) will return a cleanup
	// function referencing _the same cached executable_. So, it's fine to use
	// any of them.
	var cleanup func()

	for desc, tc := range tests {
		t.Run(desc, func(t *testing.T) {
			var resp *plugin.CodeGeneratorResponse
			var err error
			resp, cleanup, err = runWithInputFile(t, tc.filename)
			for _, cf := range tc.checks {
				cf(t, resp, err)
			}
		})
	}
	cleanup()
}

// test helpers, definitions

func noError(t *testing.T, resp *plugin.CodeGeneratorResponse, err error) {
	require.NoError(t, err)
	require.Zero(t, resp.GetError(), "expecting no error output")
}

func someError(t *testing.T, _ *plugin.CodeGeneratorResponse, err error) {
	require.Error(t, err)
}

func outputFiles(n int) checkFunc {
	return func(t *testing.T, resp *plugin.CodeGeneratorResponse, _ error) {
		files := []string{}
		for _, f := range resp.GetFile() {
			files = append(files, *f.Name)
		}
		assert.Equalf(t, n, len(resp.GetFile()), "expecting %d generated file(s), got %v", n, files)
	}
}

// This is a helper "check" to allow manually inspecting code generated in a test
func dumpFiles(t *testing.T, resp *plugin.CodeGeneratorResponse, _ error) {
	for _, f := range resp.GetFile() {
		t.Logf("%s: %s", *f.Name, f.GetContent())
	}
}

// This helper checks if the file _contains_ a substring -- it's not an exact
// match of all the contents
func fileWithContent(name string, contents ...string) checkFunc {
	return func(t *testing.T, resp *plugin.CodeGeneratorResponse, _ error) {
		for _, file := range resp.GetFile() {
			if file.GetName() == name {
				for _, content := range contents {
					require.Contains(t, file.GetContent(), content)
				}
				return
			}
		}
		t.Errorf("file %q not in response", name)
	}
}

// Helper for checking that a substring should NOT be in the file
func fileWithoutContent(name, content string) checkFunc {
	return func(t *testing.T, resp *plugin.CodeGeneratorResponse, _ error) {
		for _, file := range resp.GetFile() {
			if file.GetName() == name {
				assert.NotContains(t, file.GetContent(), content)
				return
			}
		}
		t.Errorf("file %q not in response", name)
	}
}

func syntacticallyCorrectFile(name string) checkFunc {
	return func(t *testing.T, resp *plugin.CodeGeneratorResponse, _ error) {
		for _, file := range resp.GetFile() {
			if file.GetName() == name {
				assert.NoErrorf(t, parseFileContent(file.GetContent()), "parsing file %q", name)
				return
			}
		}
		t.Errorf("file %q not in response", name)
	}
}

func parseFileContent(src string) error {
	fset := token.NewFileSet()
	_, err := parser.ParseFile(fset, "", src, parser.AllErrors)
	return err
}

type checkFunc func(*testing.T, *plugin.CodeGeneratorResponse, error)

func check(fs ...checkFunc) []checkFunc {
	return fs
}

func runWithInputFile(t *testing.T, filename string) (*plugin.CodeGeneratorResponse, func(), error) {
	files := []string{filename}
	protoFiles, err := grpctest.ParseProtoFiles(files)
	if err != nil {
		t.Fatalf("parse proto file %q: %s", filename, err)
	}
	return grpctest.GenerateCode(t, "policy", protoFiles)
}
