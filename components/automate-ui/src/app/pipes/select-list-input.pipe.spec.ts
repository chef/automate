import { SelectListInputPipe } from './select-list-input.pipe';

describe('SelectListInputPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectListInputPipe();
    expect(pipe).toBeTruthy();
  });
});
