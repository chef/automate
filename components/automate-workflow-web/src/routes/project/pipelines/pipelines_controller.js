import ng from 'angular';

function pipelinesController() {}

export default ng
  .module('cd.routes.project.pipelines.controller', [])
  .controller('pipelinesController', pipelinesController)
  .name;
