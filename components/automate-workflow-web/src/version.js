const major = 0;
const minor = 0;
const patch = 0;
const alias = 'judo-woodchuck';

const version = {
  major: major,
  minor: minor,
  patch: patch,
  alias: alias,
  full: `${major}.${minor}.${patch}-${alias}`
};

export default version;
