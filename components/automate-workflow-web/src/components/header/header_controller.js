import ng from 'angular';

headerController.$inject = ['$scope'];

function headerController($scope) {
  
}

export default ng
  .module('cd.components.header.controller', [])
  .controller('headerController', headerController)
  .name;
