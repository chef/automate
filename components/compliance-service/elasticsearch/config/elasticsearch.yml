cluster:
  name: ${es.cluster.name:chef-insights}
  initial_master_nodes: ${es.cluster.name:cses}
  routing:
    allocation:
      disk:
        threshold_enabled: false

node.name: cses

network:
  host: ${es.network.host:0.0.0.0}

path:
  data: ${es.path.data:/usr/share/elasticsearch/data}
  logs: ${es.path.logs:/usr/share/elasticsearch/logs}
